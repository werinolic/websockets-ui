import {OperationResponse, User} from "./@types.js";

export const addShips = (user: User | null, stringData: string | undefined ): OperationResponse => {
  const data =

  return { user, data: [], users: [] }
}